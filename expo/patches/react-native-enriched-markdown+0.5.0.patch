diff --git a/node_modules/react-native-enriched-markdown/ios/EnrichedMarkdown.mm b/node_modules/react-native-enriched-markdown/ios/EnrichedMarkdown.mm
index 9227b78..4f0fa77 100644
--- a/node_modules/react-native-enriched-markdown/ios/EnrichedMarkdown.mm
+++ b/node_modules/react-native-enriched-markdown/ios/EnrichedMarkdown.mm
@@ -740,7 +740,7 @@ - (void)textTapped:(ENRMTapRecognizer *)recognizer
             if (eventEmitter) {
               eventEmitter->onTaskListItemPress({
                   .index = (int)index,
-                  .checked = checked,
+                  .checked = static_cast<bool>(checked),
                   .text = std::string([itemText UTF8String] ?: ""),
               });
             }
diff --git a/node_modules/react-native-enriched-markdown/ios/EnrichedMarkdownText.mm b/node_modules/react-native-enriched-markdown/ios/EnrichedMarkdownText.mm
index 239e435..442fff5 100644
--- a/node_modules/react-native-enriched-markdown/ios/EnrichedMarkdownText.mm
+++ b/node_modules/react-native-enriched-markdown/ios/EnrichedMarkdownText.mm
@@ -524,7 +524,7 @@ - (void)textTapped:(ENRMTapRecognizer *)recognizer
             if (eventEmitter) {
               eventEmitter->onTaskListItemPress({
                   .index = (int)index,
-                  .checked = checked,
+                  .checked = static_cast<bool>(checked),
                   .text = std::string([itemText UTF8String] ?: ""),
               });
             }
diff --git a/node_modules/react-native-enriched-markdown/ios/input/EnrichedMarkdownTextInput.mm b/node_modules/react-native-enriched-markdown/ios/input/EnrichedMarkdownTextInput.mm
index 96969e8..2f9a4ae 100644
--- a/node_modules/react-native-enriched-markdown/ios/input/EnrichedMarkdownTextInput.mm
+++ b/node_modules/react-native-enriched-markdown/ios/input/EnrichedMarkdownTextInput.mm
@@ -914,12 +914,12 @@ - (void)emitOnChangeState
   _prevState.initialized = YES;
 
   emitter->onChangeState({
-      .bold = {.isActive = boldActive},
-      .italic = {.isActive = italicActive},
-      .underline = {.isActive = underlineActive},
-      .strikethrough = {.isActive = strikethroughActive},
-      .spoiler = {.isActive = spoilerActive},
-      .link = {.isActive = linkActive},
+      .bold = {.isActive = static_cast<bool>(boldActive)},
+      .italic = {.isActive = static_cast<bool>(italicActive)},
+      .underline = {.isActive = static_cast<bool>(underlineActive)},
+      .strikethrough = {.isActive = static_cast<bool>(strikethroughActive)},
+      .spoiler = {.isActive = static_cast<bool>(spoilerActive)},
+      .link = {.isActive = static_cast<bool>(linkActive)},
   });
 }
 
@@ -988,12 +988,12 @@ - (void)emitContextMenuItemPress:(NSString *)itemText
       .selectionEnd = static_cast<int>(NSMaxRange(selectedRange)),
       .styleState =
           {
-              .bold = {.isActive = boldActive},
-              .italic = {.isActive = italicActive},
-              .underline = {.isActive = underlineActive},
-              .strikethrough = {.isActive = strikethroughActive},
-              .spoiler = {.isActive = spoilerActive},
-              .link = {.isActive = linkActive},
+              .bold = {.isActive = static_cast<bool>(boldActive)},
+              .italic = {.isActive = static_cast<bool>(italicActive)},
+              .underline = {.isActive = static_cast<bool>(underlineActive)},
+              .strikethrough = {.isActive = static_cast<bool>(strikethroughActive)},
+              .spoiler = {.isActive = static_cast<bool>(spoilerActive)},
+              .link = {.isActive = static_cast<bool>(linkActive)},
           },
   });
 }
diff --git a/node_modules/react-native-enriched-markdown/ios/views/ENRMContextMenuTextView+macOS.h b/node_modules/react-native-enriched-markdown/ios/views/ENRMContextMenuTextView+macOS.h
index 58d865a..f67b831 100644
--- a/node_modules/react-native-enriched-markdown/ios/views/ENRMContextMenuTextView+macOS.h
+++ b/node_modules/react-native-enriched-markdown/ios/views/ENRMContextMenuTextView+macOS.h
@@ -8,7 +8,7 @@ typedef NSMenu *_Nullable (^ENRMContextMenuProvider)(NSMenu *baseMenu, NSTextVie
 
 /// macOS-only ENRMPlatformTextView subclass that manages context menus
 /// and text deselection across sibling text views.
-@interface ENRMContextMenuTextView : ENRMPlatformTextView <NSMenuDelegate>
+@interface ENRMContextMenuTextView : ENRMPlatformTextView
 
 @property (nonatomic, copy, nullable) ENRMContextMenuProvider contextMenuProvider;
 
diff --git a/node_modules/react-native-enriched-markdown/ios/views/ENRMContextMenuTextView+macOS.m b/node_modules/react-native-enriched-markdown/ios/views/ENRMContextMenuTextView+macOS.m
index 3da7fe6..a1360be 100644
--- a/node_modules/react-native-enriched-markdown/ios/views/ENRMContextMenuTextView+macOS.m
+++ b/node_modules/react-native-enriched-markdown/ios/views/ENRMContextMenuTextView+macOS.m
@@ -10,7 +10,7 @@ - (NSScrollView *)enclosingScrollView
   return nil;
 }
 
-- (void)deselectAllInContainer
+- (void)deselectOtherTextViewsInContainer
 {
   NSView *container = self.superview.superview;
   if (!container)
@@ -20,7 +20,7 @@ - (void)deselectAllInContainer
     for (NSView *child in segment.subviews) {
       if ([child isKindOfClass:[ENRMContextMenuTextView class]]) {
         ENRMContextMenuTextView *tv = (ENRMContextMenuTextView *)child;
-        if (tv.selectedRange.length > 0) {
+        if (tv != self && tv.selectedRange.length > 0) {
           tv.selectedRange = NSMakeRange(0, 0);
         }
       }
@@ -30,21 +30,13 @@ - (void)deselectAllInContainer
 
 - (void)mouseDown:(NSEvent *)event
 {
-  [self deselectAllInContainer];
+  [self deselectOtherTextViewsInContainer];
   [super mouseDown:event];
-
-  if (self.selectedRange.length > 0) {
-    NSMenu *menu = [self menuForEvent:event];
-    if (menu) {
-      NSPoint locationInView = [self convertPoint:self.window.mouseLocationOutsideOfEventStream fromView:nil];
-      [menu popUpMenuPositioningItem:nil atLocation:locationInView inView:self];
-    }
-  }
 }
 
 - (void)rightMouseDown:(NSEvent *)event
 {
-  [self deselectAllInContainer];
+  [self deselectOtherTextViewsInContainer];
   [super rightMouseDown:event];
 }
 
@@ -76,22 +68,12 @@ - (NSMenu *)menuForEvent:(NSEvent *)event
   }
 
   if (self.contextMenuProvider && self.selectedRange.length > 0) {
-    NSMenu *customMenu = self.contextMenuProvider(baseMenu, self);
-    customMenu.delegate = self;
-    return customMenu;
+    return self.contextMenuProvider(baseMenu, self);
   }
 
   return baseMenu;
 }
 
-- (void)menuDidClose:(NSMenu *)menu
-{
-  if (menu.delegate == self) {
-    self.selectedRange = NSMakeRange(0, 0);
-    menu.delegate = nil;
-  }
-}
-
 @end
 
 #endif
diff --git a/node_modules/react-native-enriched-markdown/ios/utils/TextViewLayoutManager.mm b/node_modules/react-native-enriched-markdown/ios/utils/TextViewLayoutManager.mm
index f24ed21..25cbe9e 100644
--- a/node_modules/react-native-enriched-markdown/ios/utils/TextViewLayoutManager.mm
+++ b/node_modules/react-native-enriched-markdown/ios/utils/TextViewLayoutManager.mm
@@ -11,31 +11,29 @@
 
 - (void)drawBackgroundForGlyphRange:(NSRange)glyphsToShow atPoint:(CGPoint)origin
 {
-  // 1. UIKit standard background drawing
-  [super drawBackgroundForGlyphRange:glyphsToShow atPoint:origin];
-
   NSTextStorage *storage = self.textStorage;
-  if (!storage || storage.length == 0)
-    return;
-
-  // 2. Safely get the container and config
-  NSTextContainer *textContainer = [self textContainerForGlyphAtIndex:glyphsToShow.location effectiveRange:NULL];
-  if (!textContainer)
-    return;
-
-  StyleConfig *config = self.config;
-  if (!config)
-    return;
+  NSTextContainer *textContainer = nil;
+  StyleConfig *config = nil;
+  if (storage && storage.length > 0) {
+    textContainer = [self textContainerForGlyphAtIndex:glyphsToShow.location effectiveRange:NULL];
+    config = self.config;
+  }
 
-  // 3. Draw specialized layers
+  // Draw code backgrounds first so the native selection background remains visible.
   // We fetch the objects into local variables FIRST to ensure they don't
   // disappear mid-method if setConfig: is called on another thread.
+  if (textContainer && config) {
+    CodeBackground *codeBg = [self getCodeBackgroundWithConfig:config];
+    [codeBg drawBackgroundsForGlyphRange:glyphsToShow layoutManager:self textContainer:textContainer atPoint:origin];
+
+    CodeBlockBackground *codeBlockBg = [self getCodeBlockBackgroundWithConfig:config];
+    [codeBlockBg drawBackgroundsForGlyphRange:glyphsToShow layoutManager:self textContainer:textContainer atPoint:origin];
+  }
 
-  CodeBackground *codeBg = [self getCodeBackgroundWithConfig:config];
-  [codeBg drawBackgroundsForGlyphRange:glyphsToShow layoutManager:self textContainer:textContainer atPoint:origin];
+  [super drawBackgroundForGlyphRange:glyphsToShow atPoint:origin];
 
-  CodeBlockBackground *codeBlockBg = [self getCodeBlockBackgroundWithConfig:config];
-  [codeBlockBg drawBackgroundsForGlyphRange:glyphsToShow layoutManager:self textContainer:textContainer atPoint:origin];
+  if (!textContainer || !config)
+    return;
 
   BlockquoteBorder *quoteBorder = [self getBlockquoteBorderWithConfig:config];
   [quoteBorder drawBordersForGlyphRange:glyphsToShow layoutManager:self textContainer:textContainer atPoint:origin];
