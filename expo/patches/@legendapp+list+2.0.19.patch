diff --git a/node_modules/@legendapp/list/index.js b/node_modules/@legendapp/list/index.js
index 246aa45..38fb5f2 100644
--- a/node_modules/@legendapp/list/index.js
+++ b/node_modules/@legendapp/list/index.js
@@ -257,7 +257,7 @@ var PositionViewState = typedMemo(function PositionView({
       ref: refView,
       style: [
         style,
-        horizontal ? { transform: [{ translateX: position }] } : { transform: [{ translateY: position }] }
+        reactNative.Platform.OS === "macos" ? horizontal ? { left: position } : { top: position } : horizontal ? { transform: [{ translateX: position }] } : { transform: [{ translateY: position }] }
       ],
       ...rest
     }
diff --git a/node_modules/@legendapp/list/index.mjs b/node_modules/@legendapp/list/index.mjs
index 5b3c6d5..6c68e43 100644
--- a/node_modules/@legendapp/list/index.mjs
+++ b/node_modules/@legendapp/list/index.mjs
@@ -236,7 +236,7 @@ var PositionViewState = typedMemo(function PositionView({
       ref: refView,
       style: [
         style,
-        horizontal ? { transform: [{ translateX: position }] } : { transform: [{ translateY: position }] }
+        Platform.OS === "macos" ? horizontal ? { left: position } : { top: position } : horizontal ? { transform: [{ translateX: position }] } : { transform: [{ translateY: position }] }
       ],
       ...rest
     }
